NixOS Configurations and deployment via Colmena.

This is the main source of truth for Maus' infrastructure (excluding any host not running NixOS)

Deployments are done whenever changes happen, because of this develop tends to reflect the current state where master is just when I remember to merge, one day I'll change this.
